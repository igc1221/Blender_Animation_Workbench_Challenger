# Blender Animation Workbench ??Challenger Review Mirror

This is a curated **public review mirror** for external web challengers.

It intentionally contains only:
- AWB Blender extension source
- automated tests
- current Rigped Phase 4 execution/product contracts
- bounded Blender runtime / worker review rules

It intentionally excludes the private repository history, build outputs, Blender vendor files,
portable user data, rolling debug logs, local worker state, and unrelated archived material.

Private source checkpoint mirrored: $SourceHead

The private AWB repository remains the source of truth.
